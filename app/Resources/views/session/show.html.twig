{% extends "::base.html.twig" %}
{% set currentUrl = url('session_show', {'id': session.id}) %}
{% block title %}{{ session.title }} | {{ parent() }}{% endblock %}

{% block body %}
    <section class="session-show interior-content clearfix">

        <h1 class="pagetitle cutout">{{ session.title }}</h1>
        {% if session.speakers %}
        <p class="attribution">
            Presented by:
            {% for speaker in session.speakers %}
                <a href="{{ path('speaker_show', {id: speaker.id}) }}">{{ speaker.fullName }}</a>{% if not loop.last %}, {% endif %}
            {% endfor %}
        </p>
        {% endif %}

        <div class="abstract">{{ session.abstractMarkdown|raw }}</div>

    </section>
{% endblock %}
